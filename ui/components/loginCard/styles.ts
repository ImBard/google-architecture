import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 40,
    borderWidth: 1,
    borderColor: 'gray',
    padding: 8,
    borderRadius: 4,
    color: 'white',
  },
  button: {
    backgroundColor: '#61dafb',
    padding: 8,
    borderRadius: 4,
    alignItems: 'center',
  },
  stepContainer: {
    gap: 8,
    marginBottom: 8,
  },
});